name: atsleadtracker
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.12+13

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  url_launcher: ^6.3.2
  pdf: ^3.11.3
  printing: ^5.14.3
  # 1.x requires vector_math ^2.2 (Matrix4.translateByDouble); Flutter SDK pins 2.1.4.
  fl_chart: ^0.69.2
  intl: ^0.20.2
  table_calendar: ^3.2.0
  excel: ^4.0.6
  file_picker: ^11.0.2
  archive: ^3.6.1
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  flutter_local_notifications: ^20.1.0
  timezone: ^0.10.1
  flutter_timezone: ^5.0.2
  firebase_storage: ^12.4.10
  # Server-pushed follow-up reminders: these arrive even when the Android app
  # is backgrounded or fully killed, unlike the on-device alarms in
  # ReminderService which only get (re)armed while the app is running.
  firebase_messaging: ^15.1.6
  # Used by DuplicateLeadService to look across every owner's leads without
  # widening the client's Firestore read permissions.
  cloud_functions: ^5.2.0
  file_save_directory: ^1.0.4
  shared_preferences: ^2.5.3
  shared_preferences_web: ^2.4.3
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.2
  flutter_native_splash: ^2.4.4

# Branded launch screen. Regenerate with:
#   flutter pub run flutter_native_splash:create
# Web is left off so the existing index.html loader keeps working.
flutter_native_splash:
  color: "#0B1020"
  image: Assets/Logo/Logo.png
  android_12:
    # Android 12+ crops the splash icon into a circle, so keep it small and
    # centred rather than letting the wordmark get clipped.
    color: "#0B1020"
    image: Assets/Logo/Logo.png
    icon_background_color: "#0B1020"
  web: false

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "Assets/ats_crm_icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "Assets/ats_crm_icon.png"
    background_color: "#0B1020"
    theme_color: "#0B1020"
  windows:
    generate: true
    image_path: "Assets/ats_crm_icon.png"
    icon_size: 256 # min:48, max:256, default: 48

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - Assets/Logo/Logo.png
    - Assets/Logo/ATS Logo.jpeg
    - Assets/Logo/ATS_Globe_Logo.png
    # Combined globe + wordmark lockup for the ATS quote letterhead, trimmed of
    # the transparent padding in "logo Header.png" so it aligns predictably.
    # The _hires variant is the same lockup taken from the client's own
    # letterhead: far sharper, but a noticeably wider wordmark. Both are
    # shipped so PdfService._atsHeaderLockupAsset can be switched in one line.
    - Assets/Logo/ats_header_lockup.png
    - Assets/Logo/ats_header_lockup_hires.png
    # ATEPL quote letterhead crops from reference AEQ 0302 — same "embed
    # artwork" approach as the ATS header lockup (fonts/colours match print).
    - Assets/Logo/atepl_header_letterhead.png
    - Assets/Logo/atepl_footer_banner.png
    - Assets/digital_signature.jpeg
    - Assets/fonts/NotoSans-Regular.ttf
    - Assets/fonts/NotoSans-Bold.ttf
    - Assets/fonts/ats/
    - Assets/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
